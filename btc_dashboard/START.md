# Bitcoin Dashboard — Quick Start

```bash
cd btc_dashboard
npm run dev          # starts server (port 3003) + client dev (port 3002)
```

Then open: http://localhost:3002

Production build:
```bash
npm run build        # builds client to client/dist/
npm start            # serves everything from port 3003
```
